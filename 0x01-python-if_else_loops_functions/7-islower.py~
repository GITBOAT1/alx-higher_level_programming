#!/usr/bin/python3
''' A function that checks for lowercase character '''

def islower(c):
    if int(c) >= 97 and <= 122:
        return True
    else if int(c) >= 65 and <= 90:
        return False
