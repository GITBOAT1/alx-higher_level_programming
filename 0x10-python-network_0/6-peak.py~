#!/usr/bin/python3
""" Find a peak """

def peak(lis):
    """ a function that finds a peak in a list of unsorted integers """
    if len(lis) > 0:
        lis.sort()
       return(lis[len(lis)]);
    else:
        return

if __name__ == __name__:
    peak()
